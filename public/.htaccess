<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si existe archivo o carpeta física, no redirigir
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Front controller
    RewriteRule ^ index.php [L,QSA]
</IfModule>
